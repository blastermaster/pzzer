# 🛍️ Парсер товаров ZZER

Гибкий парсер товаров с платформы ZZER (second-hand маркетплейс) с поддержкой конфигурации для разных категорий и брендов.

## 📋 Описание

Парсер получает данные товаров напрямую из API `gwapi-v2.goshare2.com` через Playwright, выполняя запросы в контексте браузера для обхода защиты.

**Возможности:**
- ✅ Парсинг по брендам и категориям
- ✅ Гибкая настройка через `config.json`
- ✅ Сохранение ссылок на изображения в JSON
- ✅ Детальная информация из карточек товаров
- ✅ Автоматический перевод с китайского на русский
- ✅ Пагинация (автозагрузка нескольких страниц)
- ✅ Поддержка нескольких задач

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установка Python пакетов
pip3 install --break-system-packages -r requirements.txt

# Установка браузера Chromium
playwright install chromium
```

### 2. Настройка задачи в config.json

Откройте `config.json` и настройте задачу:

```json
{
  "tasks": [
    {
      "name": "Бренд ID 223",
      "enabled": true,
      "endpoint": "brand_products",
      "payload": {
        "brandId": "223",
        "page": 1,
        "size": 20
      }
    }
  ]
}
```

### 3. Запуск парсера

```bash
# Выполнить все enabled задачи
python3 parser.py

# Выполнить конкретную задачу
python3 parser.py --task "Бренд ID 223"

# Показать список задач
python3 parser.py --list
```

## 📁 Структура проекта

```
zzer/
├── parser.py              # Основной парсер с поддержкой конфигурации
├── config.json           # Конфигурация задач и параметров
├── requirements.txt      # Зависимости
└── README.md            # Документация
```

### После парсинга создаются:

```
products/
├── brand_223.json      # JSON для бренда с ID 223
├── brand_456.json      # JSON для бренда с ID 456
└── ...
```

**Формат имени файла:** `brand_<brandId>.json`  
При каждом запуске файл перезаписывается актуальными данными.

## 📄 Описание файлов

### `parser.py`
**Главный скрипт парсера**

Что делает:
- Читает конфигурацию из `config.json`
- Запускает Playwright с headless Chrome
- Выполняет API запросы к нужным endpoint'ам с пагинацией
- Парсит товары (название, цену, фото, модель)
- Получает детальную информацию из карточек товаров
- Переводит китайские параметры на русский
- Сохраняет все ссылки на изображения в JSON
- Конвертирует цены (CNY → RUB с наценкой 35%)
- Сохраняет JSON с полными данными

**Параметры командной строки:**
```bash
--config FILE    # Путь к конфигу (по умолчанию config.json)
--task NAME      # Выполнить конкретную задачу
--list           # Показать список задач
```

**Примеры:**
```bash
# Парсинг задачи "Бренд ID 223"
python3 parser.py --task "Бренд ID 223"

# Использовать другой конфиг
python3 parser.py --config my_config.json

# Список всех задач
python3 parser.py --list
```

### `config.json` (1.5 KB)
**Файл конфигурации**

Структура:

```json
{
  "api": {
    "base_url": "https://gwapi-v2.goshare2.com",
    "endpoints": {
      "product_list": "/esbiz/api/v1/product/panHuo/productList",
      "brand_products": "/esbiz/api/v1/product/brand/productList"
    },
    "image_cdn": "https://img.goshare2.com"
  },
  "device": {
    "deviceId": "d984853b-d964-4596-8517-e4479ed65228",
    "h5Version": "5.14.11",
    "version": "7.45.0"
  },
  "parsing": {
    "max_products": 50,
    "page_size": 20
  },
  "tasks": [
    {
      "name": "Название задачи",
      "enabled": true,
      "endpoint": "brand_products",
      "payload": {
        "brandId": "223",
        "page": 1,
        "size": 20
      }
    }
  ]
}
```

**Секции конфигурации:**

#### `api` - Настройки API
- `base_url` - Базовый URL API
- `endpoints` - Доступные endpoint'ы
- `image_cdn` - CDN для изображений

#### `device` - Параметры устройства
Используются в каждом API запросе:
- `deviceId` - ID устройства
- `h5Version` - Версия H5
- `version` - Версия приложения
- `mpb`, `mpm`, `mt`, `plat`, `fmt`, `langType` - Технические параметры

#### `parsing` - Настройки парсинга
- `max_products` - Максимум товаров для парсинга
- `page_size` - Размер страницы API

#### `tasks` - Список задач
Массив задач для парсинга:
- `name` - Название задачи
- `enabled` - Выполнять ли задачу (true/false)
- `endpoint` - Какой endpoint использовать
- `payload` - Параметры запроса

### `requirements.txt`
**Зависимости проекта**

```
playwright>=1.40.0       # Браузер для обхода защиты
requests>=2.31.0         # HTTP запросы
deep-translator>=1.11.0  # Перевод китайского на русский
```

## 🎯 Примеры использования

### Парсинг конкретного бренда

1. Добавьте задачу в `config.json`:

```json
{
  "name": "Nike товары",
  "enabled": true,
  "endpoint": "brand_products",
  "payload": {
    "brandId": "456",
    "page": 1,
    "size": 20,
    "sort": "",
    "sortRule": 0
  }
}
```

2. Запустите:
```bash
python3 parser.py --task "Nike товары"
```

### Парсинг нескольких брендов

Создайте несколько задач с `enabled: true`:

```json
{
  "tasks": [
    {
      "name": "Prada",
      "enabled": true,
      "endpoint": "brand_products",
      "payload": {"brandId": "223"}
    },
    {
      "name": "Louis Vuitton",
      "enabled": true,
      "endpoint": "brand_products",
      "payload": {"brandId": "100"}
    }
  ]
}
```

Запустите без параметров:
```bash
python3 parser.py
```

### Парсинг всех товаров

```json
{
  "name": "Все товары",
  "enabled": true,
  "endpoint": "product_list",
  "payload": {
    "page": 1,
    "pageSize": 20,
    "sortType": 1
  }
}
```

### Изменение количества товаров

В `config.json`:
```json
{
  "parsing": {
    "max_products": 50
  }
}
```

## 📤 Формат выходных данных

### JSON файл
```json
{
  "id": "2350645226458865669",
  "sku": "251118781531628",
  "name": "Vanity Case牛仔布双Clogo双C标识斜挎包单肩包",
  "description": "9.5新",
  "price": "18610",
  "price_rub": "301482.00",
  "original_price": "19000",
  "original_price_rub": "307800.00",
  "market_price": "28500",
  "currency": "CNY",
  "brand": "Chanel",
  "condition": "9.5新",
  "details": {
    "Серия": "Vanity Case",
    "Серийный номер": "251118781531628",
    "Материал": "джинсовая ткань",
    "Вес": "390g",
    "Размеры": "Длина корпуса сумки 17см. / Высота корпуса сумки 19см. / Толщина корпуса сумки 13.5см. / Длина плечевого ремня 115см.",
    "Комплект": "мешок для пыли / Удостоверение личности"
  },
  "main_image": "https://img.goshare2.com/...",
  "all_images": [
    {"url": "https://img.goshare2.com/...", "is_main": true},
    {"url": "https://img.goshare2.com/...", "is_main": false},
    {"url": "https://img.goshare2.com/...", "is_main": false}
  ]
}
```

**Особенности:**
- Цены указаны в юанях (CNY) и рублях (RUB) с наценкой ×12 ×1.35
- Автоматический перевод китайских параметров на русский через Google Translate
- Запятые в китайских текстах заменяются на " - " для корректного отображения
- Все изображения сохранены как ссылки в поле `all_images` с пометкой главного (`is_main: true`)
- Поддержка пагинации - автоматическая загрузка нескольких страниц до достижения `max_products`

## 🔧 API Endpoints

### 1. Список всех товаров
```
POST /esbiz/api/v1/product/panHuo/productList
```
Параметры:
- `page` - Номер страницы
- `pageSize` - Размер страницы
- `sortType` - Тип сортировки

### 2. Товары бренда
```
POST /esbiz/api/v1/product/brand/productList
```
Параметры:
- `brandId` - ID бренда (обязательно)
- `page` - Номер страницы
- `size` - Размер страницы
- `sort` - Сортировка
- `sortRule` - Правило сортировки
- `ppath` - Путь категории

## 📊 Пример вывода

```
============================================================
Задача: Бренд ID 223
============================================================

Запуск браузера...
Открытие: https://mix.goshare2.com/
✓ Splash экран закрыт

API запрос: https://gwapi-v2.goshare2.com/esbiz/api/v1/product/brand/productList
✓ Получено товаров: 20

============================================================
Обработка 10 товаров...
============================================================

1. 拼色格纹拉链羽绒服M码 - ¥11.12
2. 毛绒混纺纯色围巾 - ¥19.12
3. Petite Malle Souple牛皮老花铆钉边角单肩包 - ¥113.6
...

✓ JSON: products/brand_223.json

============================================================
📊 Итого:
   Товаров: 50
   Изображений (ссылок): 150
   Файл: products/brand_223.json
============================================================

✅ Парсинг завершен!
```

## 🔍 Troubleshooting

### Ошибка: "Module not found"
```bash
pip3 install -r requirements.txt
playwright install chromium
```

### Пустой список товаров
Проверьте параметры в `config.json`:
- `brandId` должен быть корректным
- API может быть недоступно в вашем регионе

### Задача не выполняется
```bash
# Проверьте список задач
python3 parser.py --list

# Убедитесь что enabled: true
```

## 💡 Советы

1. **Как найти brandId?**
   - Откройте DevTools в браузере
   - Перейдите на страницу бренда на mix.goshare2.com
   - Во вкладке Network найдите запрос к `/brand/productList`
   - Скопируйте `brandId` из payload

2. **Парсинг большого количества товаров:**
   - Увеличьте `max_products` в config.json
   - Добавьте задержки между запросами
   - Используйте несколько задач для разных страниц

3. **Сохранение во внешнюю БД:**
   - Используйте JSON файл как источник данных
   - Создайте свой скрипт для импорта

## 📝 Лицензия

Создано в образовательных целях. Используйте в соответствии с правилами платформы ZZER.

---

**Автор:** Создано с помощью Claude AI  
**Дата:** 2025-11-21  
**Версия:** 4.0

**Changelog 4.0:**
- ✅ Упрощенная структура: `products/brand_<id>.json`
- ✅ Только JSON (CSV удален)
- ✅ Ссылки на изображения вместо скачивания
- ✅ Пагинация - автозагрузка нескольких страниц
- ✅ Автоматический перевод китайского на русский
- ✅ Цены в юанях и рублях с наценкой 35%
- ✅ Детальная информация из карточек товара
